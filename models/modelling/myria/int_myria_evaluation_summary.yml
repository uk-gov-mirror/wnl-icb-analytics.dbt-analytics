models:
  - name: int_myria_evaluation_summary
    description: A table holding aggregated cost and activity data for control and intervention pre and post onboarding (67 days+) using the PROPENSITY matched cohort.

    config:
      meta:
        owner:
          name: KateHomer55
    tests:
      - dbt_expectations.expect_table_row_count_to_be_between:
          arguments:
            min_value: 1